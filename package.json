{
  "name": "nodejs-7.1",
  "version": "1.0.0",
  "description": "Creamos un proyecto con un server.js en que cuando ejecutemos `$ npm run start` nos devuelva un resultado al azar entre 0 y el número que pedimos por consola.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "scanf": "^1.0.2"
  }
}
